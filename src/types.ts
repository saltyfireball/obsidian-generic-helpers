export interface GenericHelpersSettings {
	// Reserved for future helpers. No user-configurable options yet.
	_placeholder?: never;
}

export const DEFAULT_SETTINGS: GenericHelpersSettings = {};

export const MAX_HEADING_LEVEL = 6;
